"""팩 품질 게이트 — 산출물이 계약을 지키는지 판정한다.

여기 있는 것은 **팩 종류와 무관한** 구조 검사다. 팩별 도메인 명세(어느 팩이 어떤 출처를
가져야 하는가 등)는 호출자 리포에 남는다 — 그것을 이쪽으로 들이면 패키지가 특정
팩 컬렉션을 알게 되어 단방향 의존이 깨진다.

각 모듈은 **판정만 하고 출력하지 않는다.** 형식·종료코드·argv 는 호출자의 CLI 몫이다.
게이트가 print 를 들고 있으면 다른 게이트나 파이프라인이 결과를 재사용할 수 없다.
"""
