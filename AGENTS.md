# AGENTS.md

OpenCrab은 OpenClaw 계열 에이전트 환경에 MetaOntology OS 문법을 노출하는 MCP
서버다. 온톨로지 저장(그래프/문서/벡터 스토어), 팩 수집, 게이트 검증 도구를
포함한다.

게이트(설치, 린트, 테스트, 회귀 대사)를 실제로 재현하는 원시 명령은 전부
[`docs/gate-recipes.md`](docs/gate-recipes.md)에 있다. 명령을 추측하지 말고
그 문서를 먼저 읽는다.

## #154 재발 방지 규율

머신 고유 절대경로(`/home/<사용자명>/...` 등)와 사설망 IP를 공개 텍스트(문서,
커밋 메시지, PR 본문, 코드 주석)에 남기지 않는다. 코드가 강제하는 루프백
주소(`127.0.0.1` 등)는 예외다. 경로·호스트명·포트·사용자명은 역할명으로
일반화한다.
